version: 2

models:
  - name: bronze_products
    columns:
      - name: category
        data_tests:
          - accepted_values:
              values: ["Gizmo", "Doohickey","Gadget","Widget"]
      - name: id
        data_tests:
          - relationships:
              field: product_id
              to: ref('bronze_orders')